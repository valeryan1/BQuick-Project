@{
    ViewData["Title"] = "Rfq";
}

<main class="contentt px-1 py-4 mx-3">
    <div class="container-fluid mt-4">
        <h2 class="mb-4"><i class='bx bx-file'></i> RFQ Quotation</h2>
        <a class="btn btn-danger mb-3" asp-area="" asp-controller="RFQ" asp-action="CreateRFQ"><i class='bx bx-plus'></i> Create RFQ</a>
        <table class="table table-bordered">
            <thead class="table-dark">
                <tr>
                    <th>RFQ Code</th>
                    <th>Request Date</th>
                    <th>Due Date</th>
                    <th>Customer Code</th>
                    <th>RFQ Name</th>
                    <th>Total</th>
                    <th>Status</th>
                </tr>
            </thead>
            <tbody>
                @* @foreach (var rfq in Model)
                {
                    <tr>
                        <td>@rfq.RFQCode</td>
                        <td>@rfq.RequestDate.ToString("dd-MM-yy")</td>
                        <td>@rfq.DueDate.ToString("dd-MM-yy")</td>
                        <td>@rfq.CustomerCode</td>
                        <td>@rfq.RFQName</td>
                        <td>@rfq.Total.ToString("C", new System.Globalization.CultureInfo("id-ID"))</td>
                        <td>
                            <span class="status-badge @(rfq.Status == "Ready to Submit" ? "status-ready" : rfq.Status.Contains("Waiting") ? "status-waiting" : "status-open")">
                                @rfq.Status
                            </span>
                        </td>
                    </tr>
                } *@
            </tbody>
        </table>
    </div>
</main>
           

